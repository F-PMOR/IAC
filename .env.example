# ============================================
# FICHIER D'EXEMPLE POUR .env
# ============================================
# 📋 Copiez ce fichier vers .env :
#    cp .env.example .env
#
# ⚠️  Ce fichier contient uniquement les variables NON SENSIBLES
# ⚠️  Les secrets doivent être dans .env.secrets (voir .env.secrets.example)
# ============================================

# ============================================
# PYTHON
# ============================================
PYTHONUNBUFFERED=1

# ============================================
# PROXMOX (legacy - pour compatibilité)
# ============================================
PROXMOX_VERIFY_SSL=false

# ============================================
# NOTES
# ============================================
# Pour configurer les credentials, copiez .env.secrets.example vers .env.secrets
# et remplissez avec vos vraies valeurs :
#
#   cp .env.secrets.example .env.secrets
#   chmod 600 .env.secrets
#   # Editez .env.secrets avec vos credentials
#
# Variables sensibles disponibles :
# - PROXMOX_VE_* (Proxmox API credentials)
# - VSPHERE_* (VMware API credentials)
# - TF_VAR_cloud_init_* (Mots de passe VMs)
# - MYSQL_ROOT_PASSWORD (MySQL root password)
# - DOLIBARR_DB_PASSWORD (Dolibarr DB password)
